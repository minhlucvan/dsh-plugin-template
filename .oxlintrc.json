{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "categories": {
    "correctness": "error",
    "suspicious": "error",
    "pedantic": "error",
    "perf": "error",
    "restriction": "error",
    "style": "error"
  },
  "plugins": [
    "import",
    "typescript",
    "node",
    "unicorn",
    "vitest",
    "promise",
    "eslint",
    "oxc"
  ],
  "options": {
    "reportUnusedDisableDirectives": "warn",
    "typeAware": true
  },
  "env": {
    "builtin": true
  },
  "ignorePatterns": [
    "**/lib/**",
    "**/node_modules/**",
    "tests/fixtures/**",
    "packages/*/tests/fixtures/**",
    "**/*.js",
    "**/*.mjs",
    "**/*.config.ts"
  ],
  "rules": {
    "no-inline-comments": "off", // Pedantic
    "typescript/prefer-readonly-parameter-types": "off", // Pedantic

    // 与 typescript/prefer-optional-chain 冲突
    "oxc/no-optional-chaining": "off", // Restriction
    // 与 unicorn/prefer-spread 冲突
    "oxc/no-rest-spread-properties": "off", // Restriction
    // 与 promise/prefer-await-to-callbacks、unicorn/prefer-top-level-await 冲突
    "oxc/no-async-await": "off", // Restriction
    // 与 vitest/prefer-importing-vitest-globals 冲突
    "vitest/no-importing-vitest-globals": "off", // Restriction
    // 与 eslint/no-void、unicorn/no-typeof-undefined 冲突
    "eslint/no-undefined": "off", // Restriction
    "import/no-nodejs-modules": "off", // Restriction
    "node/no-sync": "off", // Restriction

    // 与 fmt.sortImports.ignoreCase 冲突
    "sort-imports": ["error", { "ignoreDeclarationSort": true }], // Style
    // 与 import/consistent-type-specifier-style 冲突
    "no-duplicate-imports": ["error", { "allowSeparateTypeImports": true }], // Style
    // 与 import/no-default-export 冲突
    "import/prefer-default-export": "off", // Style
    // 与 import/no-default-export 冲突
    "import/no-named-export": "off", // Style
    "func-style": "off", // Style
    "sort-keys": "off", // Style
    // 与 eslint/no-ternary 冲突
    "unicorn/prefer-ternary": "off", // Style
    // 与 vitest/require-hook 冲突
    "vitest/no-hooks": "off", // Style

    // Browser event handlers are synchronous by contract, so an async handler
    // hands React a promise it ignores. `void promise()` is the standard way to
    // say "deliberately not awaited", but the bare rule bans the operator
    // outright — which would leave no way to write a fire-and-forget call that
    // satisfies no-floating-promises without a `.then` chain that
    // promise/prefer-await-to-then rejects. Allowing it as a *statement* keeps
    // both type rules on and the intent explicit at the call site.
    "no-void": ["error", { "allowAsStatement": true }], // Restriction

    // The host's browser API is callback-based by design — `slots.inject(name,
    // callback)` runs the callback once the slot exists, which is the only way
    // to register before that point. Neither the real seam nor a faithful fake
    // for it can avoid taking a callback, and re-shaping a host contract to suit
    // a lint rule is not this package's call to make.
    "promise/prefer-await-to-callbacks": "off", // Restriction

    "eslint/max-lines": [
      "error",
      {
        "max": 350
      }
    ], // Style
    "eslint/max-lines-per-function": [
      "error",
      {
        "IIFEs": true,
        "max": 120
      }
    ], // Style
    "eslint/max-statements": [
      "error",
      60
    ], // Style
  }
}
